<ul>
  <li><strong>Public methods</strong>: can be called by anyone
  <li><strong>Protected methods</strong>: can only be called by objects of the defining class and its subclasses. Access kept within the family (hierarchy)
  <li><strong>Private methods</strong>: cannot be called with an explicit receiver. Since you can't define a receiver, private methods can only be called by in the defining class and by direct descendants within that same object
</ul>